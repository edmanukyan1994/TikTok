# railway.toml

[build]
  image = "node:18"
  commands = [
    "npm install",
    "npm run build",
  ]

[deploy]
  command = "node server.js"

[env]
  TELEGRAM_BOT_TOKEN = "<YOUR_TELEGRAM_BOT_TOKEN>"
  CHAT_ID = "<YOUR_CHAT_ID>"
